[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run codegen"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/health"
healthcheckTimeout = 3600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
