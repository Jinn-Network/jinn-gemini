[build]
builder = "NIXPACKS"
nixpacksConfigPath = "nixpacks.toml"

[deploy]
startCommand = "npx tsx control-api/server.ts & sleep 3 && node dist/worker/mech_worker.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
