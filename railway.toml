[build]
builder = "NIXPACKS"
nixpacksConfigPath = "nixpacks.toml"

[deploy]
startCommand = "node dist/control-api/server.js & sleep 2 && node dist/worker/mech_worker.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
