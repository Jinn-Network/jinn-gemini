{
  "name": "jinn-ponder",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node -e \"try{require('better-sqlite3');console.log('better-sqlite3 ok')}catch(e){process.exitCode=0}\" || npm rebuild better-sqlite3",
    "dev": "ponder dev",
    "start": "ponder start",
    "build": "ponder build",
    "postinstall": "npm rebuild better-sqlite3 || true"
  },
  "dependencies": {
    "@ponder/core": "^0.6.11",
    "hono": "^4.9.7",
    "viem": "^2.35.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.8"
  },
  "resolutions": {
    "vite": "^5.4.20",
    "esbuild": "^0.25.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
