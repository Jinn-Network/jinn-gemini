# Control API service build configuration
# Pin to nixpkgs archive that includes Node.js 22
[phases.setup]
nixpkgsArchive = "51ad838b03a05b1de6f9f2a0fffecee64a9788ee"

[phases.install]
cmds = ["yarn install --frozen-lockfile"]

[phases.build]
# Build jinn-node and symlink it so control-api can import 'jinn-node/...'
cmds = ["yarn run build", "ln -sf /app/jinn-node /app/node_modules/jinn-node"]

[start]
cmd = "npx tsx control-api/server.ts"
